apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mtl-app.fullname" . }}-test-connection"
  labels:
    {{- include "mtl-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mtl-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
